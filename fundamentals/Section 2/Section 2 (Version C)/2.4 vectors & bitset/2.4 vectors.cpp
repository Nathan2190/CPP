#include <bits/stdc++.h>

using namespace std;

int main() {
    vector <int> num = {1, 2, 3, 4, 5};
    cout << num.front() << endl;
    cout << num.back() << endl;
    sort(num.begin(), num.end());
    
    return 0;
}