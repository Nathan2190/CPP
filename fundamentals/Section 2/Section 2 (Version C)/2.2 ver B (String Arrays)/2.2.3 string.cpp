#include <bits/stdc++.h>

using namespace std;

int main() {
    string word = "test \ntest\n" ;
    word = word + "Add string";
    cout << "After combining string: " << word << endl;
    cout << word << endl;

    // str = "TE" + "ST";
    // cout << str.length() << endl;

    return 0;
}