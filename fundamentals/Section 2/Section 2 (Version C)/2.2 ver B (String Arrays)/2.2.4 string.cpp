#include <bits/stdc++.h>

using namespace std;

int main() {
    str = str.insert(5, "insertion");
    cout << "After insert: " << str << endl;

    str = str.substr(0, 3);
    cout << "After substr: " << str << endl;

    return 0;
}