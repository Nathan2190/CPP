#include <bits/stdc++.h>

using namespace std;

int main() {
    size_t found.idx = str.find("hello");

    if(fount.idx != string::npos)
        cout << "Found at: " << found.idx << endl;
    else
        cout << "not found!" << endl;
    
    int i = 1;
    if(i == found)
        cout << "equal" << endl;
    return 0;
}