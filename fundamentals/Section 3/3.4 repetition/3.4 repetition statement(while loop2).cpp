#include <bits/stdc++.h>

using namespace std;

int main() {
    char input;
    while (cin >> input) {
        cout << "with in the loop" << endl;

        if (input == 'Q')
        break;
    }  
    return 0;
}