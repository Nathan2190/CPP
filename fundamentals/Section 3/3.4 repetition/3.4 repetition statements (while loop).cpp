#include <bits/stdc++.h>

using namespace std;

int main() {
    int x = 0;
    while (x < 10)
    {
        cout << "x is smaller than 10" << endl;
        cout << x << endl;
        x++;
    }
    bool condition = true;
    while (condition)
    {
        
    }
    

    return 0;
}