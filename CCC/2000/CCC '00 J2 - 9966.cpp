#include <bits/stdc++.h>

using namespace std;

int main() {
    int start, end, upside_down_count;
    cin >> start >> end;
    if (start > 0){
        start++;
            if(start == )
                upside_down_count ++;
                    if (start == end){
                    cout << upside_down_count << endl;
                }
    }
    return 0;
}